version: '3.8'

services:
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    image: vedana-api:latest
    ports:
      - '3002:3002'
    restart: always

  web:
    build:
      context: ./client
      dockerfile: Dockerfile
    image: vedana-web:latest
    ports:
      - '8082:80'
    depends_on:
      - api
    restart: always

services:
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    dns:
      - 8.8.8.8
      - 8.8.4.4
    image: vedana-api:latest
    ports:
      - '3002:3002'
    restart: always

  web:
    build:
      context: ./client
      dockerfile: Dockerfile
    image: vedana-web:latest
    ports:
      - '8082:80'
    depends_on:
      - api
    restart: always
